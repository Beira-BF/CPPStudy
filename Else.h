#pragma once
#include <string>
#include <deque>
int Abs(int &);
int Max(int, int);
int Min(int, int);
int Com_divisor_max(int, int);
int Com_multiple_min(int, int);
int power(int, int);
std::string Int_to_string (int);